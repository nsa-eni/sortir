{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Pseudo</th>
                <th>Firstname</th>
                <th>Name</th>
                <th>Phone</th>
                <th>Email</th>
                <th>Actif</th>
                <th>Administrator</th>
                <th>Images</th>

                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.pseudo }}</td>
                <td>{{ user.firstname }}</td>
                <td>{{ user.name }}</td>
                <td>{{ user.phone }}</td>
                <td>{{ user.email }}</td>
                {#   <td>{{ user.password }}</td> #}
                  <td>{{ user.actif ? 'Yes' : 'No' }}</td>
                  <td>{{ user.administrator ? 'Yes' : 'No' }}</td>
                {#  {% if tabDetail.imageFilename %}
                      <p class="card-text text-left"><img class="image-fluid" src="{{ asset('uploads/images/' ~ tabDetail.imageFilename) }}" alt="image annonce" height="200"/></p>
                  {% endif %}   #}


               {#   <td>{{ user.roles ? user.roles|json_encode : '' }}</td> #}






                <td>
                    <a href="{{ path('user_show', {'id': user.id}) }}">show</a>
                    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('user_new') }}">Create new</a>
{% endblock %}
