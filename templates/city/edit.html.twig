{% extends 'base.html.twig' %}

{% block title %}Modification Ville{% endblock %}

{% block body %}
    <br>
    <div class="offset-1 mr-5">
    <h1 class="text-center">Modification d'une ville</h1>
    <br>

        {{ form_start(form) }}
        <div class="form-group col-md-2">
            {{ form_label(form.name) }}
            <br>
            {{ form_widget(form.name) }}
        </div>
        <div class="form-group col-md-2">
            {{ form_label(form.zipCode) }}
            <br>
            {{ form_widget(form.zipCode) }}
        </div>
        <br>
        <div class="form-group col-md-2">
            {{ form_widget(form.submit,{'attr': {'class': 'form-control btn-outline-primary'}}) }}
        </div>
        {{ form_end(form) }}
        <br>
        {% if not form.vars.valid %}
            {# Loop through every form item #}
            {% for child in form.children %}
                {# Display the errors of the form item #}
                {% if child.vars.errors is defined %}
                    {% for error in child.vars.errors %}
                        <div class="alert alert-danger" role="alert">
                            {{ error.message }}
                        </div>
                    {% endfor %}
                {% endif %}

            {% endfor %}
        {% endif %}
        <br>
        <a href="{{ path('city_index') }}">Retour à la liste des villes</a>
    </div>

{% endblock %}
