{% extends('base.html.twig') %}

{% block body %}
    <div class="grid-container">
        <div class="grid-x grid-padding-x">
            <a class="small-12 medium-8 medium-offset-2 large-6 large-offset-3 cell">

                <h1>Renouvellement du mot de passe</h1>
                {{ form(form) }}

                {# <a href="{{path('reset_password') }}" class="at30 col s12"Modifier mon mot de passe</a>#}
             {#reset_password#}
            </div>
        </div>
    </div>
{% endblock %}